server:
  port: 8081


spring:
  profiles:
    include: mail,session

customer:
  msg: jack
  property:
  - first-name: zhang
    last-name: san
  - first-name: liu
    last-name: sheng

module:
  shiro:
    cacheTime: 1080
    tokenSecret: J(#_=*!A
    tokenPrefix: "shiro:token:${server.port}:"
    anonUrls:
      - /api/login
      - /api/logout
      - /api/sign/test
    authUrls: /**



#swagger:
#  butler:
#    resources[0]:
#      name: service-a
#      url: /service-a/v2/api-docs
#      swagger-version: 2.0
#    resources[1]:
#      name: service-b
#      url: /service-b/v2/api-docs
#      swagger-version: 2.0
#  show: true

springfox:
  documentation:
    swagger-ui:
      enabled: true

